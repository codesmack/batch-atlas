import { HighligterDirective } from './highligter.directive';

describe('HighligterDirective', () => {
  it('should create an instance', () => {
    const directive = new HighligterDirective();
    expect(directive).toBeTruthy();
  });
});
